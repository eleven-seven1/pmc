=========================================================================================

系统框架 : SpringBoot(2.0.2.RELEASE) + SpringCloud(Finchley.RC2) + myBatis(2.0.1)

==========================================================================================

模块概述 :
    -- 公共模块
        pmc-common : 包含工具类 枚举值 等
        pmc-dao : 项目持久层
        pmc-pojo :  实体类
        pmc-interface : 业务层接口

    -- 业务模块
        pmc-eureka : SpringCloud注册中心
        pmc-provider : 微服务提供模块
        pmc-webapp : 微服务消费模块

===========================================================================================